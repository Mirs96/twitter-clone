export interface FollowedUserDetails {
    id: number; // Follow relationship ID
    userNickname: string; // Nickname of the user being followed
    userProfilePicture: string;
    createdAt: string; 
}